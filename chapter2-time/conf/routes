# TODO: Add routes as follows:
#
#  - /time        should call TimeController.time
#  - /time/<zone> should call TimeController.timeIn(...)
#  - /time/zones  should call TimeController.zones

GET         /time           controllers.TimeController.time
GET         /time/zones     controllers.TimeController.zones
GET         /time/:zone     controllers.TimeController.timeIn(zone:String)
